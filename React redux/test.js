import React from "react";

const Users = () => {
  const checkHandler = () => {
    if (
      username === "parth" &&
      password === "parth123" &&
      url === "google.omc"
    ) {
      console.log("correct");
    } else [console.log("in correct")];
  };
};

export default Users;
